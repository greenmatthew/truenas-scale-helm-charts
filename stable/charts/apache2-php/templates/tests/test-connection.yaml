apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apache2-php.fullname" . }}-test-connection"
  labels:
    {{- include "apache2-php.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apache2-php.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
