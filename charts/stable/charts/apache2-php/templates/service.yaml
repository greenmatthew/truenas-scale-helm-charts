apiVersion: v1
kind: Service
metadata:
  name: {{ include "apache2-php.fullname" . }}
  labels:
    {{- include "apache2-php.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apache2-php.selectorLabels" . | nindent 4 }}
